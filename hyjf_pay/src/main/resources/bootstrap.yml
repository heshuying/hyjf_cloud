spring:
  application:
    name: PAY

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 9092

logging:
  file: pay.log