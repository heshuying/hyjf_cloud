spring:
  application:
    name: ZUUL

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 8080

logging:
  file: zuul.log