spring:
  application:
    name: AM-CONFIG

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 8091

logging:
  file: am-config.log