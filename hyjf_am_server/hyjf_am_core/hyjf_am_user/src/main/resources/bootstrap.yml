spring:
  application:
    name: AM-USER
  cloud:
    config:
      uri: http://micoserver:2222/
      name: am-user
  boot:
    admin:
      client:
        url: ${boot.admin.clientUrl}
# zipkin 监控端点
  zipkin: 
    base-url: http://micoserver:9411
  sleuth:
    sampler:
    percentage: 0.1
    
  profiles:
    active: dev
server:
  port: 8092

# spring boot admin 监控的端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

logging:
  file: am-user.log
  file.max-size: 100MB