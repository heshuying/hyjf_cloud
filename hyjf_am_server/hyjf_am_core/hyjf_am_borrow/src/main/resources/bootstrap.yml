spring:
  application:
    name: AM-BORROW

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 8096

logging:
  file: am-borrow.log