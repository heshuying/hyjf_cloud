spring:
  application:
    name: SIGNATURES
  cloud:
    config:
      uri: http://micoserver:2222/
      name: hyjf-signatures
  boot:
    admin:
      client:
        url: http://micoserver:18080
# zipkin 监控端点
  zipkin: 
    base-url: http://micoserver:9411
  sleuth:
    sampler:
    percentage: 0.1
    
  profiles:
    active: beta

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA.CLIENT.SERVICEURL}
#  instance:
#    preferIpAddress: true

server:
  port: 9095

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
