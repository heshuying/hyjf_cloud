spring:
  application:
    name: BATCH
  cloud:
    config:
      uri: http://micoserver:2222/
  profiles:
    active: dev
server:
  port: 9091
  servlet:
    jsp:
      class-name: org.apache.jasper.servlet.JspServlet

logging:
  file: batch.log