spring:
  application:
    name: CALLCENTER

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 9093

logging:
  file: cs-user.log