spring:
  application:
    name: CALLCENTER
  cloud:
    config:
      uri: http://gitlab.hyjf.com:6080/microserver/micro-deploy.git
      profile: test
      name: callcenter

server:
  port: 9094

logging:
  file: callcenter.log