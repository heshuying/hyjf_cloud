spring:
  application:
    name: CALLCENTER
  cloud:
    config:
      uri: http://micoserver:2222/
      profile: test
      name: callcenter

server:
  port: 9094

logging:
  file: callcenter.log
#swagger启用开关
swagger:
 enable: true