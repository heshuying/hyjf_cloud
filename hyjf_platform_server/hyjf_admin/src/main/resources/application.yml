spring:  # Redis
  redis:
    #数据库索引
    database: 0
    # Redis服务器地址
    host: 123.56.216.212
    # Redis服务器连接端口
    port: 6666
    # Redis服务器连接密码（默认为空）
    password: RH06863b3e6
    pool:
      jedis:
        #最大连接数
        max-active: 8
        #最大阻塞等待时间(负数表示没限制)
        max-wait: -1
        #最大空闲
        max-idle: 8
        #最小空闲
        min-idle: 0
    # 连接超时时间（毫秒）
    timeout: 5000

# 没有密码？？ namesrvAddr: ${rocketMQ.namesrvAddr}
rocketMQ:
  namesrvAddr: 123.56.216.212:5555
  
# Redis
redis:
  ip: 123.56.216.212
  port: 6666
  pool:
    maxActive:  1024
    maxIdle: 200
    maxWait: 10000
    testOnBorrow: true
    testOnReturn: true
    password: RH06863b3e6
