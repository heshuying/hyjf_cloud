spring:
  application:
    name: CS-MESSAGE

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 8084

logging:
  file: am-message.log
