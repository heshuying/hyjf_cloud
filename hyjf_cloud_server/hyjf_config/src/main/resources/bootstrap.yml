spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: http://10.10.2.12/java_new/micro-deploy.git
          searchPaths: src/main/resources
          username: zhangqingqing
          password: zqq123456

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true

server:
  port: 2222

logging:
  file: config.log