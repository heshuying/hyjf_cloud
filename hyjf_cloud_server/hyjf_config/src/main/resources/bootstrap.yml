spring:
  application:
    name: CONFIG_SERVER
  cloud:
    config:
      server:
        git:
          uri: http://10.10.2.12/java_new/configRepository.git
          ##searchPaths: config_files
          username: xiashuqing@shizitegong.com
          password: xsq123456

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true

server:
  port: 2222

logging:
  file: config.log