spring:
  application:
    name: CONFIG-SERVER
#  profiles:
#    active: native
  cloud:
    config:
      server:
#        native:
#          searchLocations: D:/hyjf/git/0709/micro-deploy-master-9f3d4cab6ee35303fc7a0498ac61120addd0005a/src/main/resources
        git:
          uri: http://gitlab.hyjf.com:6080/microserver/micro-deploy.git
          searchPaths: src/main/resources
          username: deployment
          password: deploy2018

eureka:
  client:
    serviceUrl:
      defaultZone: http://micoserver:1111/eureka/
  instance:
    preferIpAddress: true

server:
  port: 2222

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

logging:
  file: config.log
