spring:
  application:
    name: config-server
  boot:
    admin:
      client:
        url: http://MONITOR-SERVER
#  profiles:
#    active: native
  cloud:
    config:
      server:
#        native:
#          searchLocations: D:/hyjf/git/1030/micro-deploy/src/main/resources
        git:
          uri: http://gitlab.hyjf.com:6080/microserver/micro-deploy.git
          searchPaths: src/main/resources
          username: deployment
          password: deploy2018
  profiles:
    active: beta

eureka:
  client:
    serviceUrl:
      defaultZone: http://microserver1:1111/eureka/,http://microserver2:1111/eureka/
  instance:
    preferIpAddress: true

server:
  port: 2222

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

logging:
  file: config.log
  file.max-size: 100MB
