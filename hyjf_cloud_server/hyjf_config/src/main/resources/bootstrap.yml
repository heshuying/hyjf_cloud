spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          #青岛用
          uri: http://10.10.2.12/java_new/micro-deploy.git
          #上海用
          #uri: http://219.147.28.242:3333/java_new/micro-deploy.git
          searchPaths: src/main/resources
          username: sunshuaishuai
          password: sss123456

eureka:
  client:
    serviceUrl:
      defaultZone: http://micoserver:1111/eureka/
  instance:
    preferIpAddress: true

server:
  port: 2222

logging:
  file: config.log
