spring:
  application:
    name: CONFIG

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    preferIpAddress: true
server:
  port: 2222

logging:
  file: config.log